---
description: Stage, commit, and push changes to the repository
---

1. Stage all changes using `git add .`
2. Commit changes with a generated descriptive message based on recent work.
// turbo
3. Push changes to the remote repository using `git push`
